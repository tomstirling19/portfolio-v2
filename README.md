# Portfolio v2

Thomas Stirling's portfolio. Next.js static export, deployed to S3 + CloudFront via GitHub Actions on push to `main`.

```bash
npm run dev      # local dev server
npm run build    # static export to ./out
npm run lint
npm run format
```
